name: CI
on:
  pull_request:
    branches:
      - main
      - master
  push:
    branches:
      - beta
      - main
      - master
permissions:
  contents: write
  id-token: write
  pull-requests: write
jobs:
  ci:
    uses: plumelo/dev/.github/workflows/node.yml@master
    secrets: inherit
